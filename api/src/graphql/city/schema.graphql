type City {
  name: String
  provinces: [Province]
}

type Query {
  getAllCities: [City!]!
  # getCity(city: String): City
}

type Mutation {
  createCity(name: String): City
  updateCity(name: String, newName: String): City
  deleteCity(name: String): City
}