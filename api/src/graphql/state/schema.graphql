type State { 
  id: ID!
  name: String
  order: String
  properties: [Property]!
  stage: Stage!
  createdAt: String
  updatedAt: String
}

type Query {
  getAllStates: [State]!
}

type Mutation {
  createState(input: StateInput): State
  updateState(name: String, input: StateInput): State
  deleteState(name: String): State
}

input StateInput {
  name: String
  order: String
  stageId: String
}