type TokenResponse {
    accessToken: String
    refreshToken: String
}

type Query {
    login(username: String, password: String): TokenResponse
    isAuth(id: ID, token: String): Boolean
    getNewAccessToken(refreshToken: String): String
}

# posibly not required mutation in this schema (?
