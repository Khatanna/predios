type UserType {
  id: ID!
  name: String!
  users: [User!]!
}

type Query {
  getAllUserTypes: [UserType!]
}

type Mutation {
  createUserType(input: CreateTypeUserInput): UserType
}

input CreateTypeUserInput {
  name: String!
}