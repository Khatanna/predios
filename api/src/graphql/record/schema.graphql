# Types
type Record {
  id: ID!
  resource: String!
  operation: String!
  description: String
  ip: String
  user: User
  action: Level
  result: String
  createdAt: String!
  updatedAt: String!
}

# Queries
type Query {
  getAllRecords(resource: String): [Record!]
}

# Mutations
# type Mutation {

# }

# Input types
