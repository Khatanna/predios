import React from 'react';

export type NotFoundPageProps = {
}

const NotFoundPage: React.FC<NotFoundPageProps> = ({ }) => {
	return <div>Recurso no existente en el sistema</div>;
};

export default NotFoundPage;
